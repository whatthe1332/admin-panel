import 'package:flutter/material.dart';

class AvailableSizeProductNotFoundPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Available Size Product Not Found'),
      ),
      body: Center(
        child: Text('Available Size Product Not Found'),
      ),
    );
  }
}
