import 'package:flutter/material.dart';

class GenderCategoryNotFoundPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gender Category Not Found'),
      ),
      body: Center(
        child: Text('Gender Category Not Found'),
      ),
    );
  }
}
