import 'package:flutter/material.dart';

class ProductCategoryNotFoundPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Product Category Not Found'),
      ),
      body: Center(
        child: Text('Product Category Not Found'),
      ),
    );
  }
}
